# DevHub 开发指南

本文面向仓库开发者与本地联调场景，说明开发环境、常用命令、联调方式与最小验证要求。

## 1. 文档边界

- 所有公开行为、序列化字段、状态机、错误码与测试断言均以 [`docs/Spec.md`](./Spec.md) 为准。
- 本文只覆盖本仓库的开发与联调流程；部署、上线与运行期排障请分别参考 [`部署与运行指南`](./部署与运行指南.md) 和 [`运维排障手册`](./运维排障手册.md)。

## 2. 开发环境

- .NET SDK `10.0+`
- Python `3.9+`
- Python 依赖：`requests`
- Windows ACL 严格校验场景额外需要：`pywin32`

示例安装命令：

```bash
python3 -m pip install requests
```

Windows 需要执行 ACL 相关集成测试时，再额外安装：

```bash
python3 -m pip install pywin32
```

## 3. 仓库结构

```text
DevHub/
├── docs/                          # 协议规范、规划与运维文档
├── src/
│   ├── DevHub.Core/               # 核心领域模型与基础服务
│   ├── DevHub.Host/               # ASP.NET Core 宿主程序
│   ├── DevHub.Tests/              # 单元测试（白盒）
│   ├── DevHub.Host.Tests/         # Host 级测试
│   └── DevHub.slnx                # 解决方案文件
├── tests/                         # Python 集成测试（黑盒）
└── temp/                          # 临时测试产物与报告
```

## 4. 常用命令

```bash
dotnet build src/DevHub.slnx -c Release
dotnet run --project src/DevHub.Host/DevHub.Host.csproj -c Release
dotnet test src/DevHub.slnx -c Release
python3 tests/test_runner.py --smoke --no-header
python3 tests/test_runner.py --full --no-header
```

说明：

- 解决方案统一使用 `.slnx`，不要引入 `.sln` 文件。
- `dotnet run` 适合本地开发与联调；发布场景请使用 [`部署与运行指南`](./部署与运行指南.md) 中的 `dotnet publish` 流程。
- `python3 tests/test_runner.py --smoke --no-header` 与 `python3 tests/test_runner.py --full --no-header` 默认针对“已启动的本地 Hub”执行；若需要隔离 Hub 夹具或自定义启动方式，请参考 [`tests/README.md`](../tests/README.md)。
- 更细的黑盒测试夹具说明见 [`tests/README.md`](../tests/README.md)。

## 5. 推荐开发流程

1. 先阅读 [`docs/Spec.md`](./Spec.md) 中对应章节，确认改动是否影响公开契约。
2. 执行 `dotnet build src/DevHub.slnx -c Release`，确保当前分支基础可构建。
3. 使用 `dotnet run --project src/DevHub.Host/DevHub.Host.csproj -c Release` 启动本地 Hub。
4. 从运行时目录中的 `hub.json` 动态读取 `httpBaseUrl`、`wsUrl` 与 `tokenFile`，禁止硬编码端口或地址。
5. 完成改动后，至少执行单元测试与 smoke 集成测试；若涉及公开接口或复杂链路，再补充更完整的黑盒回归。

## 6. 本地联调

### 6.1 启动 Host

```bash
dotnet run --project src/DevHub.Host/DevHub.Host.csproj -c Release
```

启动后，Hub 会写出发现文件 `hub.json` 与访问令牌 `token.txt`。运行时目录的默认位置与覆盖方式见 [`部署与运行指南`](./部署与运行指南.md)。

### 6.2 发现地址与 token

客户端必须以 `hub.json` 为权威来源读取下列字段：

- `httpBaseUrl`
- `wsUrl`
- `tokenFile`

如需查看当前运行时目录、发现文件和 token 的最小诊断脚本，请参考 [`运维排障手册`](./运维排障手册.md) 中“3.1 读取 `hub.json` 与 token”一节。

### 6.3 HTTP 鉴权与 `hub.ping`

以下示例中的 `DEVHUB_HTTP_BASE_URL` 与 `DEVHUB_TOKEN` 需要替换为 6.2 中从 `hub.json` 与 `tokenFile` 读取出的实际值，或先导出为同名环境变量。

```bash
curl -sS -X POST "$DEVHUB_HTTP_BASE_URL/rpc" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $DEVHUB_TOKEN" \
  -H "X-DevHub-Protocol: 1" \
  -H "X-DevHub-ClientId: QuickStartCLI" \
  -H "X-DevHub-ClientSessionId: 00000000-0000-0000-0000-000000000001" \
  -d '{
    "jsonrpc": "2.0",
    "id": "ping-1",
    "method": "hub.ping",
    "params": { "echo": "hello-devhub" }
  }'
```

期望结果示意：

```json
{
  "jsonrpc": "2.0",
  "id": "ping-1",
  "result": {
    "ok": true,
    "serverTimeUtc": "<server-time-utc>",
    "echo": "hello-devhub"
  }
}
```

### 6.4 `hub.invoke.request` 请求体示例

前提：目标 `appId` 有在线实例可 `poll/respond`；否则会返回 `-32010 instance_not_found`。

```json
{
  "jsonrpc": "2.0",
  "id": "req-1",
  "method": "hub.invoke.request",
  "params": {
    "appId": "test.app",
    "target": {
      "scope": null
    },
    "method": "test.ping",
    "args": {
      "from": "quickstart"
    },
    "options": {
      "ttlMs": 300000,
      "waitTimeoutMs": 120000,
      "queueIfOffline": true,
      "autoLaunch": true
    }
  }
}
```

### 6.5 WebSocket 鉴权与订阅

首条消息必须是带 `id` 的 `hub.ws.authenticate` 请求：

```json
{
  "jsonrpc": "2.0",
  "id": "ws-auth-1",
  "method": "hub.ws.authenticate",
  "params": {
    "token": "<token-from-hub-json.tokenFile>",
    "protocolVersion": 1,
    "clientId": "QuickStartWS",
    "clientSessionId": "00000000-0000-0000-0000-000000000002"
  }
}
```

认证成功后，可继续发送 `hub.events.subscribe` 订阅事件。

## 7. 最小验证要求

涉及协议、宿主、公开接口或运行时行为的改动，在提交前至少执行：

```bash
dotnet test src/DevHub.slnx -c Release
python3 tests/test_runner.py --smoke --no-header
```

如需完整黑盒回归，再执行：

```bash
python3 tests/test_runner.py --full --no-header
```

## 8. 相关文档

- [`docs/Spec.md`](./Spec.md)
- [`docs/部署与运行指南.md`](./部署与运行指南.md)
- [`docs/运维排障手册.md`](./运维排障手册.md)
- [`docs/DevHub协议与开发规划.md`](./DevHub协议与开发规划.md)
- [`tests/README.md`](../tests/README.md)
