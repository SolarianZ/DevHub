# DevHub 开发指导文档

本仓库包含 DevHub 协议规范、架构规划、核心实现及配套测试套件。

## 项目概述

DevHub 是一款本地单用户守护进程（Local Per-user Daemon），旨在为各类开发工具提供实例注册、发现、调用编排与事件订阅能力。项目的核心实现与测试均以 `docs/Spec.md` 作为权威技术标准。

## 项目结构

```
DevHub/
├── docs/                          # 协议规范与设计文档
│   ├── Spec.md                    # 权威技术规范（Single Source of Truth）
│   ├── DevHub协议与开发规划.md      # 架构演进与里程碑规划
│   └── DevHub_M1细化任务文档.md     # M1 阶段任务分解与验收标准
│   └── DevHub_M2细化任务文档.md     # M2 阶段任务分解与验收标准
├── src/                           # 核心实现代码（.NET）
│   ├── DevHub.Core/               # 核心领域模型与基础服务
│   ├── DevHub.Host/               # 基于 ASP.NET Core 的宿主程序
│   ├── DevHub.Tests/              # 单元测试（白盒测试）
│   └── DevHub.slnx                # 解决方案文件（统一使用 .slnx，禁止引入 .sln）
├── tests/                         # 集成测试套件（黑盒测试）
│   ├── README.md                  # 集成测试环境配置说明
│   └── test_runner.py             # 集成测试自动化入口脚本
└── LICENSE                        # 许可证文件
```

## 运行时数据规约

运行时根目录默认遵循系统约定，可通过环境变量 `DEVHUB_RUNTIME_DIR` 显式覆盖。各平台默认路径如下：

-   **Windows**: `%LOCALAPPDATA%\DevHub\`
-   **macOS**: `~/Library/Application Support/DevHub/`
-   **Linux**: `~/.local/share/DevHub/`（遵循 XDG_DATA_HOME 规范）

### 运行时目录结构

```
<DEVHUB_RUNTIME_DIR>/
├── runtime/
│   ├── hub.json          # 发现文件（存储 httpBaseUrl, wsUrl, tokenFile 等）
│   └── token.txt         # 访问令牌（权限限定为仅当前用户可读）
├── apps/
│   ├── definitions/      # AppDefinition 定义文件（*.json）
│   └── instances/        # 实例镜像
└── logs/
    └── *.log             # 系统运行日志
```

**使用约定**：
-   客户端应始终通过读取 `hub.json` 获取动态端口与服务地址，严禁硬编码固定端口。

## 技术栈与工程约束

-   **开发环境**：C# / ASP.NET Core (.NET 10)
-   **项目管理**：仅使用 `.slnx` 格式解决方案文件
-   **序列化**：System.Text.Json
-   **日志架构**：Microsoft.Extensions.Logging + Serilog
-   **测试框架**：xUnit + Moq (单元测试) / Python (集成测试)

## 开发与质量规范

### 编码规范
-   **架构优先**：注重模块化设计，明确功能分层与职责边界，拒绝碎片化代码重构。
-   **DRY 原则**：提炼通用逻辑，构建可复用组件。需审慎识别“相似”与“相同”逻辑，避免过度抽象。
-   **测试驱动**：倡导先编写白盒测试用例，后进行功能实现。

### 注释规范
-   **语言**：统一使用中文注释。
-   **API 文档**：所有公开接口（Public Interfaces）必须完备编写**文档级**注释（XML Documentation）。
-   **关键逻辑**：针对复杂算法或涉及框架底层机制的逻辑，应添加详尽的解释性注释。

### 日志规范
-   **分级记录**：严格遵守日志分级，确保日志格式统一。
-   **全链路追踪**：详细记录关键操作与异常状态，保证故障溯源路径的完整性。
-   **清理临时日志**：功能交付前，必须清理**临时**调试日志（非临时调试日志允许保留）。

### 测试规约
遵循“测试金字塔”原则：以单元测试为核心，集成测试覆盖主链路。
-   **优先级**：基于 `Spec.md`、核心业务路径、变更范围及复杂逻辑优先构建测试。

1.  **单元测试（白盒，DevHub.Tests）**
    -   **覆盖范围**：核心算法、边界条件、DTO 映射、工具类方法。
    -   **非覆盖范围**：外部依赖（应通过 Mock/Stub 隔离）、第三方库原生行为、简单属性存取。
    -   **原则**：遵循 F.I.R.S.T. 原则（快速、独立、可重复、自验证、及时）。

2.  **集成测试（黑盒，tests/）**
    -   **覆盖范围**：核心业务流（Happy Path）、异常处理机制、中间件行为、持久化逻辑及接口契约。
    -   **非覆盖范围**：函数内部实现细节。
    -   **原则**：模拟真实环境，关注业务闭环，并确保测试后环境幂等（清理测试数据）。

## 评审与优化规则
-   在保障功能稳定性的前提下，持续审查架构设计与数据链路。
-   优化职责划分，遵循 DRY 原则，但严禁将不同业务用途的相似逻辑强行合并。

## 行为准则
-   **文档先行**：任何功能变更前，必须研读 `docs/Spec.md`，确保实现符合协议规范。
-   **规范对齐**：详细字段、错误码、状态机及序列化要求必须与 `Spec.md` 严格一致。
-   **上下文感知**：根据当前 Git 分支名称判断所属里程碑，仅在该里程碑定义的范围内开发。
-   **禁止自动提交**：本地文件变更后，**禁止**通过脚本自动提交 Git ，须由开发者手动校验后提交。

## 参考资料
-   [协议规范 (docs/Spec.md)](docs/Spec.md)
-   [架构与开发规划 (docs/DevHub协议与开发规划.md)](docs/DevHub协议与开发规划.md)
-   [M2 任务细化文档 (docs/DevHub_M2细化任务文档.md)](docs/DevHub_M2细化任务文档.md)